- Para instalar nuevos paquetes

```
pnpm add nombre-paquete
```

- Si queremos instalar swagger
- npm install --save @nestjs/swagger

con pnpm:

```
pnpm add @nestjs/swagger
```

Para configurar variables de entorno:
```
pnpm add @nestjs/config
```
